export class Pose2D {
    public x: number;
    public y: number;
    public theta: number;

    constructor(x: number, y: number, theta: number) {
        this.x = x;
        this.y = y;
        this.theta = theta;
    }
}